#Features Wanted
#Adjustable Grid Size
#Beginning Squares
#Row and Column Numbers
#
#First is Rules
#Grid Size
width = int(input("enter your grid width"))
height = int(input("enter your grid height"))
board = []
for i in range(height):
  board.append(["*"] * width)
for x in board:
  grid = ("".join(x))
  print(grid)
